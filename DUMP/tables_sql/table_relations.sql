--- A relation, rid=133275|n1=141480|n2=18280|t=10|w=50
CREATE TABLE relations (
	rid INTEGER,
	n1 INTEGER,		--- NOEUD 1
	n2 INTEGER,		--- NOEUD 2
	t INTEGER,		--- relationType
	w INTEGER,
	CONSTRAINT PK_RELATIONS PRIMARY KEY (rid),
	CONSTRAINT FK_NODE_DEPART FOREIGN KEY (n1) REFERENCES nodes(eid),
	CONSTRAINT FK_NODE_ARRIVE FOREIGN KEY (n2) REFERENCES nodes(eid),
	CONSTRAINT FK_REL_TYP FOREIGN KEY (t) REFERENCES relationTypes(rtid)
);


CREATE TABLE relations (
	rid INTEGER PRIMARY KEY,
	n1 INTEGER,
	n2 INTEGER,
	t INTEGER,
	w INTEGER,
    FOREIGN KEY (n1) REFERENCES nodes(eid),
    FOREIGN KEY (n2) REFERENCES nodes(eid),
    FOREIGN KEY (t) REFERENCES relationTypes(rtid)
);